# 기여 및 변경관리 규칙

이 프로젝트는 아파트의 공식 정보와 향후 주민서비스를 다룰 수 있으므로 일반 홍보 홈페이지보다 엄격한 변경관리가 필요합니다.

## 1. 기본 원칙

- 모든 개발은 GitHub 이슈에서 시작합니다.
- 한 PR은 하나의 명확한 목적만 처리합니다.
- 공식 정보와 실제 사진은 권한 있는 검토자의 승인을 받아야 합니다.
- 개인정보, 내부문서, 비밀키와 운영계정 정보를 공개 저장소에 올리지 않습니다.
- 배포 성공을 운영 준비 완료로 간주하지 않습니다.
- 게시기록과 권한변경은 사후에 추적할 수 있어야 합니다.

## 2. 작업 시작

1. 관련 이슈를 선택하거나 새 이슈를 만듭니다.
2. 이슈에 사용자 문제, 범위, 제외범위, 완료조건을 기록합니다.
3. 총괄 방향은 Issue #12와 `docs/ROADMAP.md`를 확인합니다.
4. 공식 콘텐츠 변경이면 `docs/CONTENT_VERIFICATION.md`를 먼저 완료합니다.
5. 개인정보 또는 인증 기능이면 Issue #8의 기준을 먼저 충족합니다.

## 3. 브랜치 이름

권장 형식:

```text
fix/issue-1-css-grid
content/issue-2-verified-complex-data
feat/issue-4-public-routing
docs/issue-12-roadmap
chore/issue-10-ci
```

`main`에서 직접 기능개발하지 않습니다. 긴급한 문서 정정도 가능하면 PR로 검토합니다.

## 4. 커밋

커밋은 검토 가능한 단위로 작성합니다.

권장 예시:

```text
fix: correct invalid CSS grid properties
content: replace unverified apartment facts
docs: define resident portal boundaries
feat: add notice detail routing
test: add responsive layout checks
chore: add CI validation workflow
```

다음과 같은 메시지는 피합니다.

```text
update
fix stuff
changes
final
```

## 5. PR 범위

한 PR에 다음을 무리하게 섞지 않습니다.

- 레이아웃 수정과 공식정보 정정
- 접근성 수정과 대규모 디자인 변경
- 관리자 권한과 주민인증
- 보안설정과 새로운 기능
- 콘텐츠 승인과 무관한 코드 리팩터링

혼합이 불가피하면 이유와 검증방법을 PR에 설명합니다.

## 6. PR 필수내용

- 연결 이슈
- 변경 목적
- 변경한 파일
- 제외한 범위
- 자동검사 결과
- 수동검증 결과
- 데스크톱·모바일 화면 또는 동영상
- 접근성 영향
- 보안·개인정보 영향
- 콘텐츠 검증 및 승인 여부
- 배포·롤백 방법
- 남은 제한사항

`.github/PULL_REQUEST_TEMPLATE.md`의 체크리스트를 사용합니다.

## 7. 공식 콘텐츠 변경

다음은 코드리뷰만으로 승인할 수 없습니다.

- 단지명
- 주소
- 세대수와 동 수
- 사용승인일
- 관리사무소 연락처와 운영시간
- 시공사와 시설정보
- 공식 공지
- 공개 회의자료
- 실제 단지 사진
- 공식 로고와 영문명

PR에는 공개 가능한 범위에서 검증 출처와 검증일을 기록합니다. 개인 서명, 전화번호, 내부 결재문서 등 민감한 승인증거는 공개 저장소에 첨부하지 않고 제한된 보관소의 참조번호만 남깁니다.

## 8. 개인정보 금지사항

다음 정보를 이 저장소, 이슈, PR, 스크린샷, 테스트데이터에 올리지 않습니다.

- 주민 이름과 동·호수의 조합
- 개인 전화번호와 이메일
- 차량번호
- 민원 본문
- 서명과 도장
- 신분증
- 계정복구 정보
- 비공개 회의자료
- 수사·소송·분쟁 관련 개인정보
- 운영 비밀번호, API 키, 토큰

테스트에는 명백한 합성데이터를 사용합니다.

## 9. UI와 접근성

- 페이지 이동은 `<a>`를 사용합니다.
- 실행은 `<button>`을 사용합니다.
- 포커스 표시를 제거하지 않습니다.
- 키보드와 터치 모두 검증합니다.
- 모바일 360px부터 확인합니다.
- 색상만으로 상태를 전달하지 않습니다.
- 이미지에는 적절한 대체텍스트를 제공합니다.
- 애니메이션은 `prefers-reduced-motion`을 존중합니다.
- 한국어와 영어의 길이 차이를 확인합니다.

## 10. 보안

- 외부 라이브러리 버전을 고정합니다.
- 비밀정보를 코드에 하드코딩하지 않습니다.
- 사용자 입력을 신뢰하지 않습니다.
- 업로드 파일의 확장자, MIME, 크기와 권한을 검사합니다.
- 관리자 계정에는 MFA를 적용합니다.
- 인증·권한·감사로그 변경은 별도 보안리뷰를 받습니다.
- 보안정책이 정해지기 전에는 주민 개인정보 기능을 구현하지 않습니다.

## 11. 테스트 기준

### 정적 UI 변경

- HTML 검증
- CSS lint
- JavaScript lint
- 360, 390, 768, 1024, 1440px 확인
- 키보드 탐색
- 브라우저 콘솔 오류 없음
- 주요 링크 확인

### 데이터·콘텐츠 변경

- 출처
- 검증일
- 한국어·영어 일치
- 임시값 제거
- 개인정보 점검
- 공식 승인

### 백엔드·인증 변경

- 권한별 성공·거부 테스트
- 직접 URL 접근 테스트
- 세션만료와 로그아웃
- 감사로그
- 입력검증
- 오류시 개인정보 비노출
- 백업과 복구

## 12. 리뷰 규칙

리뷰어는 다음을 별도로 판단합니다.

1. 코드가 요구사항을 구현하는가
2. 공식 콘텐츠가 검증되었는가
3. 개인정보와 보안 경계가 유지되는가
4. 접근성과 모바일 사용성이 악화되지 않는가
5. 운영자가 실제로 사용할 수 있는가
6. 롤백할 수 있는가

코드가 정상이어도 콘텐츠 승인이나 운영절차가 없으면 병합하지 않습니다.

## 13. 병합 조건

- 연결 이슈 존재
- PR 템플릿 완료
- 필수 CI 통과
- 요청된 리뷰 완료
- 공식 콘텐츠 승인 완료
- 개인정보·보안 검토 완료
- 프리뷰 검증 완료
- 남은 제한사항 기록

향후 브랜치 보호규칙이 설정되면 필수 검사를 우회하지 않습니다.

## 14. 배포

### 프리뷰

모든 PR은 운영과 분리된 프리뷰에서 검증하는 것을 원칙으로 합니다.

### 운영

운영 배포에는 다음 기록이 필요합니다.

- commit SHA
- 승인자 역할
- 배포일시
- 운영 URL
- 자동검사 결과
- 수동검증 결과
- 롤백 대상 SHA
- 배포 후 확인결과

### 롤백

장애 또는 잘못된 콘텐츠가 발견되면 이전 검증 SHA로 되돌릴 수 있어야 합니다. 데이터 변경이 포함되면 데이터 롤백 또는 정정절차도 함께 준비합니다.

## 15. 이슈 종료

이슈는 다음이 확인된 뒤 종료합니다.

- 완료조건 충족
- 구현 PR과 SHA 연결
- 테스트 증거
- 문서 갱신
- 남은 제한사항 또는 후속 이슈
- 실제 운영 영향 확인

‘코드를 작성했다’는 사실만으로 완료 처리하지 않습니다.
